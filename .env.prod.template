# =============================================================================
# PRODUCTION ENVIRONMENT TEMPLATE
# =============================================================================
# 
# CRITICAL: This template is for production deployment.
# ALL passwords must be cryptographically secure and unique.
# Use proper secrets management (Kubernetes secrets, HashiCorp Vault, etc.)
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION (PRODUCTION)
# =============================================================================
# SECURITY: Use separate database credentials from other environments
DB_USER=prod_guardianes_user
DB_PASSWORD=GENERATE_SECURE_PASSWORD_WITH_64_CHARACTERS_MINIMUM
DB_ROOT_PASSWORD=GENERATE_SECURE_ROOT_PASSWORD_WITH_64_CHARACTERS

# Database Connection (External)
MYSQL_HOST=prod-mysql-cluster.example.com
MYSQL_PORT=3306

# =============================================================================
# MESSAGE BROKER CONFIGURATION (PRODUCTION)
# =============================================================================
RABBITMQ_USER=prod_guardianes_mq
RABBITMQ_PASSWORD=GENERATE_SECURE_RABBITMQ_PASSWORD_64_CHARS
RABBITMQ_HOST=prod-rabbitmq-cluster.example.com
RABBITMQ_PORT=5672

# =============================================================================
# CACHE CONFIGURATION (PRODUCTION)
# =============================================================================
REDIS_HOST=prod-redis-cluster.example.com
REDIS_PORT=6379

# =============================================================================
# MONITORING & OBSERVABILITY (PRODUCTION)
# =============================================================================
GRAFANA_USER=prod_monitoring_admin
GRAFANA_PASSWORD=GENERATE_SECURE_GRAFANA_PASSWORD_64_CHARS

PROMETHEUS_USERNAME=prod_metrics_collector
PROMETHEUS_PASSWORD=GENERATE_SECURE_PROMETHEUS_PASSWORD_64_CHARS

# =============================================================================
# APPLICATION SECURITY (PRODUCTION)
# =============================================================================
# CRITICAL: JWT secret must be cryptographically secure
JWT_SECRET=GENERATE_CRYPTOGRAPHICALLY_SECURE_JWT_SECRET_128_CHARACTERS_MINIMUM
JWT_EXPIRATION=3600

# CRITICAL: Remove hardcoded admin user in production
# Use proper user management system instead
ADMIN_USERNAME=NOT_USED_IN_PRODUCTION
ADMIN_PASSWORD=NOT_USED_IN_PRODUCTION

# =============================================================================
# PRODUCTION SECURITY SETTINGS
# =============================================================================
SPRING_PROFILES_ACTIVE=prod
TZ=UTC

# Rate limiting (strict in production)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=50

# SSL/TLS Configuration
DB_SSL_MODE=REQUIRED
REDIS_SSL_ENABLED=true
RABBITMQ_SSL_ENABLED=true

# =============================================================================
# PRODUCTION SECURITY REQUIREMENTS
# =============================================================================
#
# 1. PASSWORD REQUIREMENTS:
#    - Minimum 64 characters for production
#    - Use cryptographically secure random generation
#    - Rotate quarterly or immediately if compromised
#
# 2. SECRETS MANAGEMENT:
#    - Use Kubernetes secrets or HashiCorp Vault
#    - Never store secrets in container images
#    - Use secret scanning in CI/CD pipeline
#
# 3. DATABASE SECURITY:
#    - Enable SSL/TLS for all database connections
#    - Use separate database users with minimal privileges
#    - Enable database audit logging
#
# 4. MONITORING:
#    - Enable security monitoring and alerting
#    - Set up log aggregation with ELK/EFK stack
#    - Monitor for failed authentication attempts
#
# 5. NETWORK SECURITY:
#    - Use private networks for internal services
#    - Enable firewall rules restricting access
#    - Use load balancer with SSL termination
#
# =============================================================================